<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.hm.repo.OrdersRepo">

	<insert id="insert" parameterType="com.ssafy.hm.dto.Orders" useGeneratedKeys="true" keyProperty="orderId">
		insert into orders (user_id, order_store)
		values (#{userId}, #{orderStore});
	</insert>

	<select id="selectByUser" parameterType="string" resultType="com.ssafy.hm.dto.Orders">
		select order_id as orderId, user_id as userId, order_store as orderStore, order_received as orderReceived, order_time as orderTime, order_received_time as orderReceivedTime
		from orders
		where user_id = #{userId}
    	order by order_time desc;
	</select>
	
	<select id="selectAll" resultType="com.ssafy.hm.dto.Orders">
		select order_id as orderId, user_id as userId, order_store as orderStore, order_received as orderReceived, order_time as orderTime, order_received_time as orderReceivedTime
		from orders
    	order by order_time desc;
	</select>
	
	<update id="receiveOrder" parameterType="int">
	    update orders
	    set order_received = true, order_received_time = current_timestamp
	    where order_id = #{orderId}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from orders where order_id = #{orderId};
	</delete>

</mapper>
