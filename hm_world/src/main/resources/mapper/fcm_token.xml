<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.hm.repo.FcmTokenRepo">
    <insert id="upsert" parameterType="com.ssafy.hm.dto.FcmTokenRequest">
        INSERT INTO fcm_token (token, user_id)
        VALUES (#{token}, #{userId})
        ON DUPLICATE KEY UPDATE
            user_id = VALUES(user_id),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <select id="selectTokensByUserIds" resultType="string">
        SELECT token
        FROM fcm_token
        WHERE user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectTokensByTicketTrue" resultType="string">
        SELECT t.token
        FROM fcm_token t
        JOIN account a ON a.user_id = t.user_id
        WHERE a.ticket = true
    </select>

    <select id="selectTokensByUserIdsAndTicketTrue" resultType="string">
        SELECT t.token
        FROM fcm_token t
        JOIN account a ON a.user_id = t.user_id
        WHERE a.ticket = true
          AND t.user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
