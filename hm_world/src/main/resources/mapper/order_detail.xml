<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.hm.repo.OrderDetailRepo">

	<insert id="insert" parameterType="com.ssafy.hm.dto.OrderDetail" useGeneratedKeys="true" keyProperty="detailId">
		insert into order_detail (order_id, item_id, order_quantity)
		values (#{orderId}, #{itemId}, #{orderQuantity});
	</insert>

	<select id="selectByOrder" parameterType="int" resultType="com.ssafy.hm.dto.OrderDetail">
		select detail_id as detailId, order_id as orderId, item_id as itemId, order_quantity as orderQuantity
		from order_detail
		where order_id = #{orderId};
	</select>

</mapper>
